const Submission = require("../models/Submission");

module.exports = {
    startExam : async (data) => {
        try {

        }
        catch(err) {
            return {
                error: err,
            }
        }
    }
}